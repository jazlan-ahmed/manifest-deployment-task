{
  "name": "manifest-deployment-app",
  "version": "1.0.0",
  "description": "Sample application for manifest deployment via GitHub Actions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Building application...' && mkdir -p dist && cp index.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["github-actions", "deployment", "manifest"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}